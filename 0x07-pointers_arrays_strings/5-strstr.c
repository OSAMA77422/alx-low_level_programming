#include "main.h"

/**
 * *_strstr - i
 *
 * @haystack: vfd
 * @needle: fevdf
 *
 * Return: vdf
*/

char *_strstr(char *haystack, char *needle)
{
for (; *haystack != '\0'; haystack++)
{
	char *o = haystack;
	char *t = needle;
	while (*one = *two && *two != '\0')
	{
		one++;
		two++;
	}

if (needle == '\0')
{
return (haystack);
}
}

return (NULL);
}
