#include "main.h"

/**
 * *_strstr - i
 *
 * @haystack: vfd
 * @needle: fevdf
 *
 * Return: vdf
*/

char *_strstr(char *haystack, char *needle)
{
int i, j = 0;
for (x = 0; haystack[x] != 0; x++)
{
for (i = 0; haystack[x] = needle[i]; i++)
{
j++;
}
}
for (i = j; haystack[i] != '\0'; i++)
{
return (haystack);

}
return ('\0');
}
