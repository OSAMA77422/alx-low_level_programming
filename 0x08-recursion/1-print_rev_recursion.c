#include "main.h"

/**
 * _strlen_recursion - vdfv
 *
 * @s: vdf
 * Return: vfv
*/

void _print_rev_recursion(char *s)
{
int x, y = 0;

for (x = 0; s[x] != '\0'; x++)
{
y++;
}
for (x = y - 1; x >= 0; x--)
{
putchar (*s);
}
}
