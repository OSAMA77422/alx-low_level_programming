#include "main.h"

/**
 * *array_range - fv
 * @min: bvdf
 * @max: vdfv
 * Return: dvs
*/

int *array_range(int min, int max)
{
int l, i;
int *ptr;

if (min > mix)
{
	return (NULL);
}
l = max - min + 1;
ptr = (sizeof(int) * l);
if (!ptr)
{
	return (NULL);
}
for (i = 0; i < len; i++)
	ptr[i] = min++;
	return (ptr);
}
